# 按具体替换识别重复建议

## 本地处理

对明确的英文“原文 → 替换”和“可改为：替换句”进行有上限的词序列对齐，提取原文位置、删除词和新增词。只有原文位置与替换内容相同才算覆盖，不依赖类别名称。支持短语建议中的斜线备选表达。

当整句建议的所有替换均被已有短语建议覆盖时，去掉整句重复记录；部分覆盖、不同替换目标或额外否定词的建议保留。跨轮匹配同样使用明确替换证据。

## 截图解读修正

第14项 invented information → fabricated information 与旧短语建议相同。
第13项并非完全重复：整句还把 gives 改为 provides，并调整了 students can 等结构。不能为了减少数量删除全部额外修改；当前保留部分覆盖的建议，不声称它是完全独立的新问题，也不把额外替换判为必要或正确。

## 验证与限制

完整 verify:release 通过，包含既有19组流程测试和新增跨类别、合并替换、备选表达、输入顺序、部分覆盖、相反目标与否定词负例。不发送付费模型请求。

仅识别明确且可解析的词级替换；自由解释、复杂语义等价和标点级修改不靠此规则合并。无法可靠解析时沿用既有比较逻辑。部分重复建议仍会保留整条显示，尚未拆成只包含新增修改的卡片。

本次未提交、推送或部署，真实模型表现需发布后验证。
