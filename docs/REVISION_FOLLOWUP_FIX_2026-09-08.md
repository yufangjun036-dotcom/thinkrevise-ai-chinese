# 第二轮复检一致性修复

## 证据与范围

用户确认只将 students is 改成 students are；截图显示未修改的 checking the answer careful 被列为未再检出，并将重合段落的论证建议列为新的段落衔接建议。未取得本次原始响应及初稿全部建议，不能断言每一项截图分类都已完成语义核验。

## 修复

- 将“词性”纳入词形类别：所在句未变时，保留旧的客观词形错误；真正改为 carefully 时不恢复旧引文。
- 跨类别论证/解释/衔接匹配要求长引文连续重合，并且建议共享原因解释、证据或例子意图。同位置但不同建议不强行归并。
- 长引文去重不再仅比较长度，必须有实际文本包含关系，避免无关长句被删除。
- 引文已以句号结束时，提取上下文不再误包含下一句，避免下一句的修改导致旧错误失去保护。

## 验证边界

回归脚本新增词性遗漏、真实修正、跨类别匹配、不同建议负例、无关长句去重及完整句边界用例。使用合成响应调用实际处理函数，不使用付费模型。

跨类别识别仍是保守启发式，不是完整语义匹配；未再检出仍不代表保证正确。当前仅本地修改，需用户确认后提交和部署，再验证真实模型输出。
