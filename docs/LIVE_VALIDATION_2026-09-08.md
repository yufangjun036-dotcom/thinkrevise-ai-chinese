# 真实线上验证与验收门槛

用户要求：以后要求用户验证之前，助手必须先自行验证。不能把离线合成测试或部署完成当作真实 AI 流程验收。

## 本次自行执行

- 已读取线上当前结果页面：用户结果为12项首次诊断、3项未再检出、9项仍存在、7项补充发现。
- 随后通过生产 /api/coach 执行真实初稿请求，并将原返回反馈传入第二稿请求。唯一修改 students is → students are。
- 两次生产请求均 HTTP 200、provider=openai。没有读取或展示 API 密钥，没有绕过限流，没有重试模型请求。
- 测试脚本最初误检查 source 字段，初稿请求返回后停止；修正为 provider 后复用已保存的初稿结果，仅继续第二稿，未重复收费请求。
- 本次接口结果：首次12、未再检出1（students is）、仍存在11、修改位置0、补充0；careful 和 teh 均保留。
- 原始处理后响应保存在 scripts/live-incident-initial-2026-09-08.json 与 scripts/live-incident-revision-2026-09-08.json，均为公开测试作文数据，不含密钥。这不是上游未经处理的模型响应。

## 验收结论：整体仍未通过

单轮基础检查通过不能推翻用户失败证据。用户页面明确仍有带引号替换未合并、分类变化和旧问题漏配。当前自行接口测试与用户页面结果不同，说明尚不能认定稳定。未自行操作完整前端输入流程，不声称端到端 UI 测试已通过。

后续顺序：保留真实失败样例、修复、离线回放、真实接口多场景验证、浏览器完整流程检查、人工审阅问题与最终稿，最后才请用户验收。需要费用或权限且无法执行时，明确报告未验证，不把测试负担转给用户。

本次未修改生产业务逻辑，未提交或部署新版本。
