# Portfolio images

The PNG files in this directory are captured from an isolated local production build with `THINKREVISE_DEMO_MODE=1`.

- Use only synthetic classroom text.
- Exclude browser chrome, accounts, credentials, local URLs and personal information.
- Re-capture the images after material interface changes.
